# HANDOFF · 文案脚本工作台（v3）

> **2026-09-18 设置与 Git 独立落位覆盖**：用户授权建库、推送并公开。应用已在原址建立独立 Git 根，远端 https://github.com/wampeeHuang/script-editor ，main 分支；父目录 D:/tools 的其他工具改动和历史不上传。设置改为模态表单，不再退出项目；列表/回收站/导入有“返回当前项目”，恢复阶段与阅读位置。详细合同见 docs/RECORDING-STAGE.md。此前关于“独立 Git 尚未执行、设置为独立页、未推送”的历史说明被本段覆盖。

> **2026-09-18 界面与源码提交**：正式运行源码在本目录，入口 8781。当前编辑页顶部为文案/声音/交付阶段，工具随阶段显隐；正文与播放条共用内容底板，淡主题色页眉保留，完整外框四角 8px。章节为独立微色差卡片，章节号 32×32px、18px 数字，句序号 12px；导航旧阴影及句旁竖色带已移除。主题变量与 Logo/favicon 同源。浏览器 29 项回归通过，目录门禁/地图、7 项治理测试和项目删除隔离测试通过；详细合同见 docs/RECORDING-STAGE.md。本次 Git 仅包含应用源码、文档和测试，用户项目/音频/导出物/本地偏好及大批历史档案不入库，不推送。声音与镜头规划的新功能尚未实现，不以界面回归代表完整 MVP 验收。

> **2026-09-18 开发目录整改覆盖说明**：当前人入口 README.md，Agent 入口 AGENTS.md，目录地图由 `checks/workspace.py` 生成；文档归入 docs/，活动验证为 `tests/smoke.py` 与 `tests/test_project_deletion.py`。108组原址移动、4786个文件在快照与移动后逐文件SHA-256核验一致，未删除历史文件或改变用户项目内容。完整恢复点 `F:/_migration-snapshot/script-editor/structure-before-20260918/`；映射与证据 `archive/structure-20260918/`。旧 `_runtime/solution-design/` 已整体移至 `archive/solution-design/`，其余历史试验/截图/补丁/夹具在 `archive/legacy-runtime/`；运行仓仅保留现用CosyVoice依赖与日志。下文的旧目录树和路径是历史记录，不作为当前目录导航。跨目录搬家及独立Git仓库尚未执行：目标工作区要求干净恢复点，现有D:/tools工作树有未提交改动，本轮不擅自提交。详见 docs/WORKSPACE-STRUCTURE.md。

> **本轮最终验证**：新增治理测试7项通过；结构门禁、README地图一致性、删除生命周期和离线烟测通过。186个受保护文件（项目数据除可变library登记、根部源码、字体与token）与快照一致；浏览器按ID打开40句文稿，项目1个/回收站0个，导入页正常，Console 0 errors / 0 warnings。清理器移除了2个只含pyc的可再生成缓存目录，未清理用户数据、历史档案或依赖。验证记录与四张截图在 archive/structure-20260918/。正式入口8781已恢复，无付费调用、提交或推送。

> **2026-09-18 本地存储合同**：见 `STORAGE-CONTRACT.md`。《掼蛋指南》原 `_runtime` 全部167个文件（138280543字节）已迁至 `projects/b160a2a69cbd4b29a77d3c4d404d4c2a`，迁移时逐文件SHA-256一致，ID不变；目录登记与 default-project.txt 更新。F盘恢复快照 `F:/_migration-snapshot/script-editor/20260918-guandan`。原位置仅保留 `.moved-project.json`，Workspace拒绝旧位置读写，防止旧标签再次落盘。验证：保存/按ID重开、旧位置409拒写、原始录音范围读取、当前与兼容各两次导出互不覆盖，以及6项导入隔离检查通过；浏览器列表打开40句文稿正常。兼容导出也改为独立子目录，其根位置仍在项目内。历史视频文件整体保留，未将旧TTS自动采用为当前旁白。外部旧脚本的硬编码路径未改，便携完整项目包与既有项目追加资料尚未实现。证据见 `_runtime/solution-design/managed-storage-20260918/`。

> **2026-09-17 当前覆盖说明**：用户决定收在录音阶段；详细当前合同、验证与限制见 [RECORDING-STAGE.md](RECORDING-STAGE.md)。本轮按用户 Descript 截图重构首页和编辑页，新增麦克风录音/导入、试听采用及真实工程音频来源。无显式保存键。以下 v3 旧交接中的四后端、行内按钮、精确逐句/零误差、五件套及旧布局描述不作为当前已验收事实。

> 本阶段源码快照提交 `faccfc4`，包含此前尚未入库的本应用后端/前端基线及本轮改造，不含其他工具改动、工程稿件或音频。未推送。复盘：`D:/workspace/_output/retrospectives/2026-09-17-script-editor-recording-stage.md`。8781 保持运行，隔离测试 8795 已停止。

> 2026-09-16。本文档是跨会话唯一交接通道，新 agent 从这里开始。
> 需求权威：**`PRD.md`（v3）**。本文档记录**已实现状态**与踩坑。
> v2（纯静态单文件编辑器）已归档到 `_runtime/v2/`（原 `文案脚本编辑器.html`）。

## 一句话

把 `脚本撰写 → 脚本修改/审核 → 配音录制 → 字幕/时间戳` 四阶段压成**一个交互循环**：
句子列表是唯一真值，改一句只重算它所在的**合成单元**，听觉反馈由免费本机朗读承担，
付费合成只在显式批量确认时发生。

## 怎么跑

```bash
python service.py                 # 默认 http://127.0.0.1:8781，自动开浏览器
python service.py --port 8790
```

前端 **由服务本体提供**（`GET /` → `文案脚本工作台.html`），因此前端与 API 同源，
**不必知道端口号**。浏览器打开 `http://127.0.0.1:8781/` 即用。

**前端三视图**（PRD §6）：
- **稿** — 唯一工作区。段卡 → 单元分组（左侧竖线颜色标 clean/脏）→ 句行。
  行内：`🔊` 本机朗读（0 成本）/ `▶` 成品 / `⟳` 试听 / 段头删段 / ＋加句 / ＋新增段落 / 撤销重做。
  右侧常驻 9:16 + 16:9 真机预览（字数灯 16 / 22 阈值，点开放大到真机尺寸，字体/字号/离底/描边/底色条拉杆）。
- **声** — 成本仪表（待合成单元/字数/预估费用/全片时长）、后端单选（成本与能否自动是后端属性）、
  待合成清单、「更新配音」显式批量（确认窗列出 N 单元 / M 字 / 预估费 / 预计耗时）、通听全片。
- **轴** — 甘特式时间轴（每行按时长占全片比例偏移，对得上顶部标尺；段内单元标句数）、
  体检（字/秒 超出 1.8~8.5 高亮，段偏移自洽）、目标时长达标建议。**时间不可手调。**

顶部工具栏：工作目录输入 + 「导出」「投放」两个动作 + 服务状态；**交付不是视图，是动作**（§6.0）。

前端工程要点（改代码前先读）：
- 刷新走 `/api/split`（不落库）或 `/api/project`（落库）。**结构签名不变时只做轻量数值刷新**，
  否则每次敲字都重建 DOM 会让输入框失焦（已踩过）。
- 重建 DOM 时会保存并恢复 `data-i` 输入框的焦点与光标位置，兜住结构性重排。
- 离线兜底：`offlineSplit()` 是后端 `split_units` 的同规则 JS 副本，仅在服务未启动时用于显示，
  在线时以后端为权威。
- 单元状态用 `class` 与文字双写；**验证时用断言比对两者，别用眼睛看颜色**
  （实测误报过两次：状态色、首句时间，断言证明是我看错）。

## 已实现（后端全链路，实测通过）

| 文件 | 职责 |
|---|---|
| `model.py` | 真值模型、**句末标点切分合成单元**、两层时间轴、补丁、**内容寻址脏检测**、时长校准 |
| `tts.py` | 后端注册表（cost/auto/pricePerK 是**后端属性**）、豆包流式试听 + 非流式定稿、command 后端接线位 |
| `service.py` | ThreadingHTTPServer + CORS + 路由 + 批量作业队列 + 端口冲突防护 |
| `exporter.py` | 五件套导出 + 投放（含备份、按段拼接为合同命名 `seg-000N.wav`） |
| `align.py` | 句级对齐（VAD + DP / 字级 LCS）。**降级为自录音等特殊场景备用**，见下 |
| `_runtime/smoke.py` | 32 项离线断言（单元切分 / 时间轴 / 补丁 / 增量 / 成本预估 / 健康检查） |
| `_runtime/api_smoke.py` | 15 项 HTTP 端点断言 |
| `文案脚本工作台.html` | **前端**（单文件、无 CDN、Vivi 令牌）。三视图 稿/声/轴 + 行内控件 + 真机字幕预览 |
| `_runtime/e2e.py` | 真实端到端：合成 → 时间轴 → 导出 → 投放 |
| `_runtime/ui_verify.js` | **前端验证**（真实 Chromium / playwright，120+ 断言 + 设计 QA + 截图） |
| `_runtime/shot_playbar.js` | 段色带 / 交给 Agent 面板的截图脚本 |
| `_runtime/import_guandan.py` | 把掼蛋真实文案+镜头导入工具（幂等，`--dry` 试跑） |
| `_runtime/shot_guandan.js` | 掼蛋工程的实拍截图脚本 |
| `_runtime/shot_settings.js` | 只截「工程抽屉 / 悬停气泡」的秒级截图脚本（不做合成） |

验证命令与最近一次结果：

```bash
python _runtime/smoke.py            # 全部通过（32 断言）
python _runtime/smoke.py --live     # 追加真实豆包合成（master 2.345s / preview 首包 0.49s）
python _runtime/api_smoke.py        # 全部通过（15 断言，需先起服务）
python _runtime/e2e.py              # 全链路通过；时间轴与成片音轨零误差
NODE_PATH=<全局npm>/node_modules node _runtime/ui_verify.js   # 界面：50 断言 + 设计 QA
```

## 界面（v3.1 重做，见 `UI-DESIGN.md`）

v3.0 的界面被判定"粗糙且逻辑紊乱"，根因是**把内部数据模型画给了用户**（段/单元/句三层盒子嵌套）。v3.1 重做信息架构：

| 决策 | 具体 |
|---|---|
| 一主两抽屉 | 只有「稿」是主界面；声 / 轴 / 预览 / 设置都是右侧抽屉，不离开稿纸 |
| 常驻播放条 | 所有播放动作收进底部 52px 播放条，进度条带**句级刻度**；行内不再有播放按钮 |
| 分组靠色带与间距 | 同一合成单元的行共享左侧连续色带 + 行紧贴，单元之间 10px 呼吸。**没有 .unit 容器** |
| 层级靠字号不靠淡化 | 句文本 17px；次级文字统一 `--ink-mid`(#4B554F)，**不再用 ink-faint 做正文** |
| 成本信号常态可见 | 顶栏「待合成 N 字」胶囊 + 待合成行色带变琥珀；只有消费动作才弹确认窗 |
| 键盘心流 | Enter 新起一句 / 空句 Backspace 删除 / ↑↓ 换行 / Space 播放 / ←→ 换句 / Ctrl+K 改这句 |

**无障碍（实测值，非估计）**：主文本 10.32:1（AAA）、时间码 5.55:1、提示 7.74:1，全部 ≥ AA；句行点击区 43px；无过小点击目标；无横向溢出。

**Vivi 令牌的补充**：原令牌只有 ink / ink-light(#6B7470) / ink-faint(#9AA39E) 三档，后两档在密集列表里分别只有 3.45:1 与 2.59:1，**不达 AA**。界面层因此派生了一个 `--ink-mid: #4B554F`（写在页面 `:root`，注释了来源）。建议后续把这一档补进 `tokens/vivi-tokens.css`。

### v3.2 信息精简（less is more，2026-09-16）

复查结论：**问题不是元素多，是同一件事被说了 3~5 遍**（播放 5 处、待合成状态 5 处）。
原则定为「每条事实只在一个地方说，且说在最靠近决策的地方」。完整删改清单见 `UI-DESIGN.md` §8。

三个结构性改动：

1. **删掉 rail 的「稿」按钮** —— 它是个按钮却不打开任何东西，是把状态做成了控件。现在 rail 只表达"哪个抽屉开着"，不亮 = 在稿纸。
2. **播放入口 5 → 1** —— 行首 ▶ / 行内朗读图标 / 播放条「朗读」「试听」全部收进底部 ▶。
3. **智能 ▶（新行为）** —— 播什么由系统决定：单元已合成播成品，改过没合成用本机朗读即时出声（0 成本）；右侧一个标签说明当前来源。
   > 顺手修掉一个真实的坑：**原来改完一句按 ▶ 会听到上一版成品音频，且毫无提示。**

其余：行内只剩 行号/文本/时间码（删行首▶、两个图标、状态点）；稿纸 footer 三个按钮 → 一个（撤销重做上顶栏）；
声抽屉 4 指标 → 一行；顶栏去产品名与「在线」文字；设置里 7 行快捷键表 → 一行。

**「试听」从播放条移到了「声」的待合成清单** —— 它不是播放动作而是生产动作（会花钱），
应该出现在"我正要决定这一单元值不值得重合成"的地方。

### v3.3 工程概念 / 保存可见 / 粘贴拆句（2026-09-16）

| 改动 | 说明 |
|---|---|
| **导出加 `文案脚本.txt`** | 纯文本、无时间码、无表格符号 —— 用来复制/发送/存档。与 `文案脚本.md`（带时间码的核对表）分工明确。导出六件套，投放仍五件套（下游管线不需要 txt） |
| **保存状态可见** | 保存仍自动（防抖 900ms）；顶栏 `● 未保存` → `保存中…` → `已保存`（1.6s 后淡出）。**离线时红字「离线 · 改动不会保存」**（原来最危险的盲区）。Ctrl+S = 立即保存 |
| **粘贴自动拆句** | 句行里粘贴一整段 → 按 换行 / 句末标点 / 逗号（长块兜底）三级拆分。首行落进当前句，其余各起一句 |
| **「工程」抽屉（原「设置」）** | 显示**实际工程路径**（`<目录>/narration/`）；「载入」→「打开其它工程…」；「视频项目」→「投放目标」且未设置时明说 |
| **删两块** | 「服务」区块（在线是常态，真出问题会在出问题处报错）、独立的快捷键表（改挂悬停气泡） |
| **修 `data-tip` 死属性** | 此前 `data-tip` 一条样式都没有，提示从未出现过。补齐后另修两坑：贴边对齐（居中会被视口裁掉）、`width: max-content`（否则被 32px 按钮容器压成一字一行） |

工程目录语义：`S.dir` 是**基目录**，工程实际落在 `<S.dir>/narration/`；默认基目录 = 工具自身
（未归属视频项目的草稿）。`投放目标` 填的是视频项目**根目录**（不是 `_runtime`），
投放时写入它的 `_runtime/{script,tts,build}`。

### v3.4 导入真实工程 / 两个出口的区分 / 默认目录（2026-09-16）

**掼蛋指南已导入**（7 段 / 30 句 / 15 合成单元 / 镜头结构原样搬运）：

- 工程落点：`D:/workspace/2026-09-14-掼蛋指南视频/_runtime/narration/`
  （`audioBase` = 该 narration 目录；工具不再是自己给自己存草稿）
- 导入脚本：`_runtime/import_guandan.py`（数据源 `script/script.json` 的段+镜头、
  `build/文案脚本.md` 的逐句换行真值；`--dry` 可试跑）
- **`meta.secondsPerChar` 用原片真实时长反推**（0.2486），所以打开时时间轴就是
  `01:36.8`，与原片 96.8s 一致 —— 否则默认 0.17 一打开就短一大截。
  真实时长仍要等「更新配音」合成后才精确。

**默认工程可配置**：`service.py --dir <path>` > `default-project.txt` > 工具自身。
`/api/health` 的 `root` 返回的是这个默认基目录（前端拿它当兜底）。

**前端兜底**：记住的目录里没有工程（一句文案都没有）→ 自动落到服务端默认工程并告知。
`_runtime/uitestN` 这种测试目录不受影响（里面有工程）。

**导出 vs 投放**（按钮文案已改）：导出写工程自己的 `exports/`（6 件，含 txt），
**不动成片**；投放写视频项目的 `_runtime/{script,tts,build}`（5 件 + 拼接 wav），**会进管线**。
按钮从「投放」改为「**投放到成片**」。

**服务状态块**：在线时整块隐藏（常亮点零信息），仅离线时显示红点 + 文字。

**投放目标自动推断**：`S.dir` 匹配 `.../_runtime` 时，投放目标取其上一层；显示「自动：与工程同处」，
并把推断值填进输入框占位值。

### v3.5 自动保存的两处失守（2026-09-16）

用户第三次问「为什么没有保存」—— 暴露的不是理解问题，是**自动保存真的有两个洞**：

| 洞 | 后果 | 补法 |
|---|---|---|
| 没有关页兜底 | 敲完最后一字 900ms 内关页 → 丢 | `pagehide`/`visibilitychange` 用 `sendBeacon` 补发 |
| 离线时直接 return | 服务没起来时写的全部内容，刷新即失 | 本机草稿（localStorage） |

**本机草稿机制**（`script_workbench_draft_v1`）：
- 编辑后 **400ms** 写本机，**900ms** 写工程；写进工程成功即清草稿（所以平时它转瞬即逝）
- 写工程失败 → 状态改口「保存失败 · 已存在本机」并保留草稿
- 下次打开做**内容指纹比对**：相同则静默清掉；不同才弹窗问「恢复 / 忽略」，**绝不悄悄覆盖**
- 指纹只比 段(id/标题/镜头数) + 句(序号+文本) + 标题 —— 不能直接 stringify，
  前端对象与服务端 normalize 后的键顺序不一致，会误判

`doSave` 也加了 try/catch：服务中途挂掉时 `api()` 会抛，原来会变成未捕获的 Promise 拒绝。

### v3.6 编辑手感 + 合成文本补标点（2026-09-16）

**编辑手感趋同「写一段话」**（原来是"一堆独立单行框"）：

| 动作 | 现在 |
|---|---|
| 句中 `Enter` | 在光标处断开：前半留原句，后半成下一句，光标落到新句开头 |
| 行首 `Backspace` | 并回上一句，光标停在接缝上（跨段时提示） |
| 行尾 `Delete` | 把下一句并进来 |
| `↑ ↓` | 保持列位置换行 |

→ **「＋ 加一句」按钮已删**（Enter 覆盖其全部用途）；「＋ 新增段落」保留。
→ 新建段时**自带一个可写空行**，`applyOps` 删除段也加了兜底（稿纸永远至少一个可写行）。

**合成文本补标点**（`model.unit_text`，显示文本一个字不动）：
- 单元内相邻行、上一行无收尾符号 → 补「，」（否则 TTS 连读成「小指南就挂…」）
- 上一行以 `~～` 收尾，或下一行以标点开头 → 不补（实测会成「小白~，」「百搭牌，，」）
- 段尾是逗号类 → 换「。」；段尾无标点 → 补「。」；段尾是 `~～` → 不补
- 新增符号集 `TONE_END` / `ANY_PUNCT`（见 `model.py` 顶部）
- 补完的效果在「声」抽屉的待合成清单里可见（那一列显示的就是合成文本）

### v3.7 删假入口 / 段色带 / 【事故】误写（2026-09-16）

**「让 agent 改这句」是假的，已删。** 队列里 7 条指令全是测试脚本写的，`done` 是测试脚本自己搬的，
**没有任何消费者** —— 而前端承诺了"agent 会读到并回写"。删掉行内指令框、播放条按钮、`/api/instructions` 端点。

**换成真的「交给 Agent」（Ctrl+K）**：工具组装「范围（整篇/当前段/当前句）+ 你的要求 + 带句 ID 的当前文案 +
回写方式」，一键复制。不假装有 AI，是一条可验证的双向通道。

**播放条段色带**：进度条上方 5px 色带，每段一格（7 个中间调循环，避开琥珀/绿两个状态色）。
点击跳段、悬停显段名、当前段高亮、**段头编号同色**（颜色才有指向）。

#### ⚠️ 事故：测试脚本误写，覆盖了用户改过的稿子

- **原因**：`api_smoke.py` 调 `/api/export` **没带 `dir=`**，而 `/api/export` 内部会
  `save_project` → 服务端**静默回落到默认工程目录**（真实视频项目）→ 测试 fixture 覆盖真实工程
- **波及**：`project.json`、`index.json`、`exports/` 同时被覆盖
- **三处修复**：
  1. 写操作（`/api/project`、`/api/export`、`/api/publish`）**必须显式带 `dir=`，否则 400**
  2. `/api/export` 与 `/api/publish` **不再落盘 `project.json`**（它们不是保存接口）
  3. 覆盖前把旧版本留一份到 `narration/.history/`（环形 20 槽，不删文件）
- **恢复**：靠 15:47:33 那次「投放」留下的 `backup/20260916-154733/`（备份里有旧 build/，
  而新写入的是用户浏览器内存里那份 40 句）→ 用 `build/文案脚本.md` 重建，**逐字一致 24/25**
- **一句话教训**：**读操作可以宽容地回落到默认值，写操作必须显式** —— 静默回落等于埋地雷。

## 三层单位（最重要，勿混）

```
段 segment   叙事 + 画面单元（挂镜头）；也是音色/后端指定单位
单元 unit    合成单元 = 段内按「句末标点 。！？ 」切出的整句  ← 决定连贯性与增量粒度
句 sentence  时间 + 字幕单元 = 用户写的一行（常按逗号断）
```

**为什么合成单位不是「句」也不是「段」**：A/B 实测（`_runtime/ab_synth2.py`）——
行尾 F0 斜率，逗号结尾处：

| 方案 | 单元 | 逗号行尾斜率 | 响度离散 |
|---|---|---|---|
| A 整段 | 1 个 | **+2.37**（承接） | 0.79 dB |
| B 逗号行逐句 | 5 个 | **−5.47**（收束 → 听感错） | 1.26 dB |
| **D 句末标点分组** | **3 个** | **+1.84** | **0.62 dB** |

D 几乎等于整段，增量却更细 → 选定 D。**无标点的行永远与相邻行粘在一起**，
绝不会出现"半句话被单独合成"（实测那种输入会让 TTS 输出不可预测）。

## 时间戳：两层，全部"构造"而来

- **骨架层** = 单元边界，由」逐单元合成 ⇒ 时长前缀和」**构造**，0 误差、零成本
- **细分层** = 单元内的逗号行，按字符权重在骨架区间内切，误差 **封顶在单元内、不跨单元累积**

→ 因此**不需要 VAD、不需要豆包时间戳、不需要 WhisperX**。`align.py` 的两条路径
实测精度不足（VAD 因豆包逗号处不停顿而漏检；faster-whisper 字级平均 239ms），
仅在自录音（音频连续、无法按单元构造边界）等场景备用。

## 成本：三级触发（PRD §4.2）

| 级 | 触发 | 范围 | 成本 |
|---|---|---|---|
| L0 朗读 | 光标停在句上 | 该句 | **0**（浏览器 speechSynthesis，不发请求） |
| L1 试听 | 点 ⟳ | 单单元 | 免费后端 0；付费后端先显字数 |
| L2 定稿 | 点「更新配音」 | 所有脏单元（去重） | 确认窗：N 单元 / M 字 / 预估费 / 耗时 |

**编辑期永不触发合成，只标脏。** 付费后端 `auto:false` 硬约束。
单价 `pricePer1kChars` 由用户填，**工具不猜**。

## 踩过的坑（务必别再踩）

1. **端口双绑**：Windows 上 `allow_reuse_address=1` 会让两个进程监听同一端口，症状是
   "接口返回了别的应用的响应"（8771 就撞上了）。已改为 `allow_reuse_address=False` +
   先探测再绑定 + 冲突自动顺延。**默认端口 8781。**
2. **临时文件删除会被 safe-delete 钩子 fail-closed 拦下**（沙箱内回收站不可用 → OSError），
   曾把整个投放打挂。`exporter._concat_units` 已改用 ffmpeg concat **滤镜**，不产生临时文件。
3. **uid 位置式会造成假脏**：改一个逗号为句号 → 重新切分 → 位置平移 → 未改内容被误判为脏。
   已改为**内容寻址**（`cache[hash]`，音频文件 `units/<hash>.wav`）。改回原文即命中缓存。
4. **时长不能取 TTS 返回值**：豆包 `sentences[-1].endTime` 不含文件尾部静音，会让时间轴
   比实际音轨短 0.2~0.5s/单元 → 成片字幕逐段漂移。已改为以 ffprobe 文件长度为准 +
   `reconcile_durations` 按文件指纹校准。
5. **真值来源**：`/api/patch` 等接口必须以**请求里传的 project** 为准，不要从磁盘读 ——
   首次使用时磁盘没有 project.json，会拿到空项目，补丁里的句号一个都对不上。
6. **后端注册表必须补全**：前端可能只传部分 `meta`，`normalize_project` 会注入
   `tts.default_backends()`，否则合成时报"未知后端"。
7. Bash 工具跑 python 偶发"输出为空、EXIT=1"，实际已执行 → 一律重定向到文件再 Read。
8. `librosa.pyin` 在语音尾部易出**八度跳变**，必须先按滑动中位数做 2 的幂归位。

## 仍未做（下一步）

- **前端 `文案脚本工作台.html`**（最大的一块）：三视图 稿/声/轴、行内 `🔊▶⟳⚠`、
  真机字幕预览（复用 v2 的双屏组件）、指令框
- L0 本机朗读（浏览器 speechSynthesis）—— 纯前端，未接
- 本地 TTS 后端接线（IndexTTS25 / CosyVoice3 已在注册表声明，`enabled:false`，
  补 `command` 模板即可启用）
- 工具架 manifest 更新（`C:\Users\Administrator\.agentboard\tools\script-editor\manifest.json`
  现描述的是 v2"纯静态、无进程无端口"，**已不准确**）

## 关键环境事实

| 事实 | 值 |
|---|---|
| 豆包可用组合 | resourceId `seed-tts-2.0` + speaker `zh_female_xiaohe_uranus_bigtts` |
| 豆包流式 | `POST /api/v3/tts/unidirectional`，首包 **0.36~0.49s**，**不返回时间戳** |
| 豆包非流式 | submit+query，返回句级+字级时间戳，延迟抖动 3~46s |
| 密钥 | 环境变量 `DOUBAO_SPEECH_API_KEY`（**已存在，勿写入任何文件**） |
| 依赖 | `requests`（装在 `~/.workbuddy/binaries/python/envs/default`）；ffmpeg 8.0.1 |
| 真实管线样本 | `D:\workspace\2026-09-14-掼蛋指南视频`（`_runtime/{script,tts,build}`） |
| 本地配音资产 | `D:\tools\{cosyvoice3,Confucius4-TTS,doubao-tts}`、`One-Click-VidGen\tools\IndexTTS25` |
